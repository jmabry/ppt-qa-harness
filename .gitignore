*.log
node_modules/
package-lock.json
package.json
*.jpg
*.pdf
*.pptx
.DS_Store

# Allow bakeoff output decks (PDF + PPTX) to be committed for review
!bakeoff/outputs/**/*.pptx
!bakeoff/outputs/**/*.pdf

# Bakeoff vendored skill repos (cloned during setup)
bakeoff/.vendor/

# Personal config overrides
bakeoff/bakeoff.config.local.json
