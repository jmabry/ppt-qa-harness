node_modules/
package-lock.json
package.json
*.pdf
*.pptx
.DS_Store

# Allow bakeoff output decks (PDF + PPTX) to be committed for review
!bakeoff/outputs/**/*.pptx
!bakeoff/outputs/**/*.pdf

# Bakeoff generated working dirs (generators + intermediate files)
bakeoff/deck-builder/
bakeoff/minimax/
bakeoff/anthropic/

# Bakeoff vendored skill repos (cloned during setup)
bakeoff/.vendor/
